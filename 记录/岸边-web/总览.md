## 📌 当前正在进行的断点（正在执行，即将 Done）

- [x] **底层基建**：通用数据清洗层（干掉 90% 垃圾导航词）。
    
- [x] **数据安全**：智能垃圾桶机制（软删除、一键恢复、7天物理蒸发）。
    
- [ ] **界面翻新**：前台主页爆改为“深Offer同款”高密度大表格（**Cursor 正在生成，待验收**）。
    

---

## 🚀 阶段一：数据含金量攻坚（縱向挖深）

_目标：让 AI 提炼的数据从“纯文本摘要”变成“可供筛选的硬核元数据”，这是未来做高级搜索和超越同行的核心。_

- [ ] **任务 1.1：Prisma 表结构扩容**
    
    - 在 `schema.prisma` 里为 `Job` 和 `RawJob` 增加三个全行业求职刚需字段：`degree` (学历门槛: String/Enum), `count` (招聘人数: Int), `hasEstablishment` (是否有编制: Boolean/String)。
        
- [ ] **任务 1.2：Dify 提示词与 JSON Output 调优**
    
    - 修改传给 Dify 的 Prompt，强制让大模型除了吐出摘要，必须精准抠出这三个标签。
        
- [ ] **任务 1.3：附件（Word/Excel）透视眼引入**
    
    - 集成 `mammoth` 和 `xlsx` 库，让爬虫能解开高校附件，并将关键词过滤后的文本和正文缝合，一并送给大模型，彻底解决“正文没字、全在附件”的痛点。
        

---

## ⚡ 阶段二：规模化与安全防线（横向扩张）

_目标：当你想把监控的高校从 3 所扩大到 50 所、甚至几百所时，确保服务器不会崩，你也不会被高校防火墙拉黑。_

- [ ] **任务 2.1：Token 碎钞机熔断防御（成本控制）**
    
    - 在爬虫将文本送给大模型之前，加入“关键词预筛选”。非核心专业的长文本直接切掉，只保留核心上下文，保护你的 API 钱包。
        
- [ ] **任务 2.2：引入 `p-limit` 并发控制队列（性能防封）**
    
    - 将爬虫的串行 `for` 循环升级为带有最高 `5` 并发限制的异步队列，在保证抓取速度的同时，绝对不给高校服务器造成自杀式袭击。
        
- [ ] **任务 2.3：学校爬虫健康巡逻雷达（地雷一）**
    
    - 将写好的 `SchoolStatus` 状态表在后台前端真正画出来。🟢/🔴 亮灯一眼看出哪所学校的 Selector 挂了，实现点对点精准维护。
        

---

## 🎨 阶段三：用户体验与高级检索（前端美化）

_目标：大后方的数据既多又精细后，在前台把生产力价值释放给用户。_

- [ ] **任务 3.1：前台多条件复合筛选（Filter）**
    
    - 基于阶段一扒出来的元数据，在前台大表格上方实现“只看事业编”、“只看硕士及以上”的一键筛选。
        
- [ ] **任务 3.2：AI 摘要的原地丝滑展开**
    
    - 用户在首页点击大表格的某一行标题时，不跳出页面，原地下沉展开展示 Dify 提炼的精美排版摘要，再次点击收起。